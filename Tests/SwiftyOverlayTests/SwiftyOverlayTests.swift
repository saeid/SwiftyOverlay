    import XCTest
    @testable import SwiftyOverlay

    final class SwiftyOverlayTests: XCTestCase {
        func testExample() {
            // This is an example of a functional test case.
            // Use XCTAssert and related functions to verify your tests produce the correct
            // results.
            XCTAssertEqual(SwiftyOverlay().text, "Hello, World!")
        }
    }
